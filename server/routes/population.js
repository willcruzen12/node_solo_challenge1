var express = require('express');
var router = express.Router();
var animals = require('./animals.js');

var animalPop = function randomNumber(1, 100){
        return Math.floor(Math.random() * (1 + max - min) + min);
    }

module.exports = router;

//incomplete
